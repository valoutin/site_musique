<form method="post" action="{{ path('news_letter_delete', {'id': news_letter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_letter.id) }}">
    <button class="btn">Delete</button>
</form>
