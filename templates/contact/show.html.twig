{% extends 'admin.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ contact.titre }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ contact.mail }}</td>
            </tr>
            <tr>
                <th>Resquest</th>
                <td>{{ contact.resquest }}</td>
            </tr>
            <tr>
                <th>Raison_sociale</th>
                <td>{{ contact.raisonSociale }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
